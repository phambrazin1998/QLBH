<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.qlbh">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.QLBH">
        <activity android:name=".activity.Thanhtoan"></activity>
        <activity android:name=".activity.Giohang" />
        <activity android:name=".activity.thongtinsp" />
        <activity android:name=".activity.Thongtin" />
        <activity android:name=".activity.Lienhe" />
        <activity android:name=".activity.Laptop" />
        <activity android:name=".activity.Dienthoai" />
        <activity android:name=".activity.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>